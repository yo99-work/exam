import 'package:flutter/material.dart';

class AppImageView extends StatelessWidget {
  const AppImageView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
